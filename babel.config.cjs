module.exports = {
    presets: [
        "@babel/preset-env",
        "@vue/babel-preset-jsx", // Add this if you are using JSX
    ],
    plugins: [
        // Add any Babel plugins if needed
    ],
};
